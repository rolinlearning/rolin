---
import { formatDate } from "../../utils/date";
interface Props {
  posts: any[];
}

const { posts } = Astro.props;
---
<section class="mt-16 space-y-12 max-w-3xl mx-auto px-6 md:px-8 lg:px-12">
  <div class="space-y-6">
    <h1 class="text-3xl md:text-4xl font-serif font-medium text-gray-900 dark:text-gray-100 tracking-tight">
      Posts
    </h1>
    <div class="text-lg leading-relaxed space-y-4">
      <p class="font-light text-gray-700 dark:text-gray-300">
        A lo largo de los años he publicado muchos posts, aqui les comparto alguno de mis favoritos:
      </p>
    </div>
  </div>

  <div class="space-y-12 divide-y divide-gray-100 dark:divide-gray-700">
    {posts.map((post) => (
      <article class="group pt-12 first:pt-0">
        <div class="space-y-4">
          <a 
            href={`/blog/${post.slug}`} 
            class="block group-hover:-translate-y-0.5 transition-transform duration-300"
          >
            <h2 class="text-xl md:text-2xl font-medium text-gray-900 dark:text-white leading-snug">
              {post.data.title}
            </h2>
          </a>
          
          <p class="text-base text-gray-600 dark:text-gray-400 leading-relaxed font-light">
            {post.data.description}
          </p>
          
          <div class="pt-2">
            <a 
              href={`/blog/${post.slug}`} 
              class="inline-flex items-center text-sm text-gray-700 hover:text-gray-900 
                     dark:text-gray-300 dark:hover:text-gray-100 transition-colors duration-300"
            >
              <span class="border-b border-gray-300 dark:border-gray-500 group-hover:border-gray-600 
                          dark:group-hover:border-gray-400 transition-colors duration-300">
                Leer este post
              </span>
              <svg 
                class="ml-1 w-4 h-4 transform group-hover:translate-x-0.5 transition-transform duration-300" 
                viewBox="0 0 20 20" 
                fill="currentColor"
              >
                <path 
                  fill-rule="evenodd" 
                  d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" 
                  clip-rule="evenodd"
                />
              </svg>
            </a>
          </div>
        </div>
      </article>
    ))}
  </div>
</section>
